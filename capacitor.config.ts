import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ioniccapacitorapp.Conversaula',
  appName: 'Conversaula',
  webDir: 'www'
};

export default config;
