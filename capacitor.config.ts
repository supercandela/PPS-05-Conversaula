import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ioniccapacitorapp.Captura&Vota',
  appName: 'Captura&Vota',
  webDir: 'www'
};

export default config;
